<div class="container">
  <div class="row">
    <% @projects.each do |project| %>
        <div class="four columns">
          <div class="portfolio_item_show">
            <%= link_to image_tag(project.image.url(:thumb)), project %>
          </div>
        </div>
    <% end %>
  </div>
</div>