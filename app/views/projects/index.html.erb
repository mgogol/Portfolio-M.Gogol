<div class="container">
  <h1 class="main_title">All Projects</h1>
  <div class="row">
    <% @projects.each do |project| %>
        <div class="four columns portfolio_item_show_column">
          <div>
            <%= link_to image_tag(project.image.url(:thumb)), project, class: 'portfolio_item_show' %>
          </div>
        </div>
    <% end %>
  </div>
</div>